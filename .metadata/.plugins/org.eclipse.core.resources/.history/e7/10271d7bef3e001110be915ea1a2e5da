spring:
  profiles:
    active: local # 기본적으로 로컬 환경을 사용하도록 설정

  # 공통 설정 (데이터 크기 등은 배포나 로컬이나 비슷하니까요)
  servlet:
    multipart:
      enabled: true
      max-file-size: 10MB

  # DB 접속 구조 (설계도)
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    # ${변수명:기본값} 문법을 쓰면 아주 세련되어 보입니다!
    url: ${DB_URL:jdbc:mysql://localhost:3306/brew_flow?allowPublicKeyRetrieval=true&useSSL=false}
    username: ${DB_USERNAME:root}
    password: ${DB_PASSWORD:}

mybatis:
  type-aliases-package: com.brewflow.backend.domain
  configuration:
    map-underscore-to-camel-case: true

# ... 나머지 공통 설정들 ...