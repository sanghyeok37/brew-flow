<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.brewflow.api.mapper.AttachmentMapper">

    <insert id="insert" parameterType="Attachment" useGeneratedKeys="true" keyProperty="attachmentId">
        INSERT INTO attachments (
            media_type, category, parent_id, original_name, stored_name, file_path, file_size
        ) VALUES (
            #{mediaType}, #{category}, #{parentId}, #{originalName}, #{storedName}, #{filePath}, #{fileSize}
        )
    </insert>

    <select id="findById" resultType="Attachment">
        SELECT * FROM attachments WHERE attachment_id = #{attachmentId}
    </select>

    <select id="findByParent" resultType="Attachment">
        SELECT * FROM attachments 
        WHERE category = #{category} AND parent_id = #{parentId}
        ORDER BY created_at DESC
    </select>

    <update id="update" parameterType="Attachment">
        UPDATE attachments
        SET stored_name = #{storedName},
            file_path = #{filePath}
        WHERE attachment_id = #{attachmentId}
    </update>

    <delete id="delete">
        DELETE FROM attachments WHERE attachment_id = #{attachmentId}
    </delete>

    <delete id="deleteByParent">
        DELETE FROM attachments WHERE category = #{category} AND parent_id = #{parentId}
    </delete>

</mapper>
